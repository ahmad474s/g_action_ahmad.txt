name: Copy Ahmad.txt File

on:
  push:
    branches:
      - main

jobs:
  copy-file:
    runs-on: self-hosted
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Clone Project 
      run: |
            cd /home/ahmad/my_scripts
            git pull origin main

